import type { ComponentType, ComponentProps } from 'svelte';
declare function renderServer<T>(Comp: T, props: T extends ComponentType<infer C> ? ComponentProps<C> : any): ComponentType;
export declare const renderComponent: typeof renderServer;
export {};
